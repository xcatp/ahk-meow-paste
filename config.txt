# 配置文件

# 自定义分组，如：names : [ project, temp, meow ]
group : @./my.txt

groupRoot : $A_ScriptDir$\group  # 分组根目录

default     : default
historyPath : $groupRoot$\history  # 自动保存目录
defaultSave : $groupRoot$\$default$  # 默认保存目录

# color, transparent, etc
frameBgc    : 53B558
frameTRP    : 100
maskBgc     : 382E0A
maskTRP     : 100
borderWidth : 1
borderColor : 0xff448E3A  # ARGB

# switches
noBgc          : 0  # 透明背景
useRandomColor : 0  # 随机背景色（不推荐）
maskIST        : 0  # 立即显示，而不是点击后
showMask       : 1
pasteIST       : 0  # 立即贴图（不能调整）
tip            : 0
autoSave       : 1  # 自动保存
recycle        : 0  # 回收站，而不是删除

# settings
trayClick  : 1
saveSuffix : .png

# hotkeys
clipHK     : !``
lastHK     : !1
cancelHK   : Esc
clearAllHK : !Esc